{
  "permissions": {
    "allow": [
      "Bash(cat en.json)",
      "Bash(cat messages/en.json)",
      "Bash(node -e \"const fs = require(''fs''); const data = JSON.parse(fs.readFileSync(''messages/en.json'', ''utf8'')); console.log(''Top-level keys:'', Object.keys(data).join('', ''));\")",
      "Bash(for lang in ar de en es fr hi it ja ko pt ru zh)",
      "Bash(do mkdir -p \"$lang\")",
      "Bash(done)",
      "Bash(node split-messages.js)",
      "Bash(node create-index-files.js)",
      "Bash(tree messages -L 2)",
      "Bash(npm run build)",
      "Bash(cat messages/en/*.json)",
      "Bash(node -e \"\nconst fs = require(''''fs'''');\nconst stdin = fs.readFileSync(0, ''''utf-8'''');\nconst parts = stdin.trim().split(/\\n(?=\\{)/);\nconst merged = {};\nparts.forEach(part => {\n  const obj = JSON.parse(part);\n  Object.assign(merged, obj);\n});\nconsole.log(JSON.stringify(merged, null, 2));\n\")"
    ],
    "deny": [],
    "ask": []
  }
}
